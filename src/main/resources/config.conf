net {
  type = mainnet
  # type = testnet
}

storage {
  # Directory for storing persistent data

  db.directory = "database",
  index.directory = "index",

  # You can custom these 14 databases' configs:

  # account, account-index, asset-issue, block, block-index,
  # block_KDB, peers, properties, recent-block, trans,
  # utxo, votes, witness, witness_schedule.

  # Otherwise, db configs will remain defualt and data will be stored in
  # the path of "output-directory" or which is set by "-d" ("--output-directory").

  # Attention: name is a required field that must be set !!!
  properties = [
    //    {
    //      name = "account",
    //      path = "storage_directory_test",
    //      createIfMissing = true,
    //      paranoidChecks = true,
    //      verifyChecksums = true,
    //      compressionType = 1,        // compressed with snappy
    //      blockSize = 4096,           // 4  KB =         4 * 1024 B
    //      writeBufferSize = 10485760, // 10 MB = 10 * 1024 * 1024 B
    //      cacheSize = 10485760,       // 10 MB = 10 * 1024 * 1024 B
    //      maxOpenFiles = 100
    //    },
    //    {
    //      name = "account-index",
    //      path = "storage_directory_test",
    //      createIfMissing = true,
    //      paranoidChecks = true,
    //      verifyChecksums = true,
    //      compressionType = 1,        // compressed with snappy
    //      blockSize = 4096,           // 4  KB =         4 * 1024 B
    //      writeBufferSize = 10485760, // 10 MB = 10 * 1024 * 1024 B
    //      cacheSize = 10485760,       // 10 MB = 10 * 1024 * 1024 B
    //      maxOpenFiles = 100
    //    },
  ]

}

node.discovery = {
  enable = false
  persist = true
  bind.ip = ""
  external.ip = null
}

node.backup {
  port = 10001
  priority = 8
  members = [
  ]
}

node {
  # trust node for solidity node
  # trustNode = "ip:port"
  trustNode = "39.106.151.237:50051"

  # expose extension api to public or not
  walletExtensionApi = true

  listen.port = 18888

  connection.timeout = 2

  tcpNettyWorkThreadNum = 0

  udpNettyWorkThreadNum = 1

  # Number of validate sign thread, default availableProcessors / 2
  # validateSignThreadNum = 16

  maxActiveNodes = 30

  maxActiveNodesWithSameIp = 2

  minParticipationRate = 0

  p2p {
    version = 20000 # 10000: mainnet; 71: testnet
  }

  active = [
    # Active establish connection in any case
    # Sample entries:
    # "ip:port",
    # "ip:port"
  "39.106.151.237:18888",
  "39.106.164.249:18888",
  "39.106.65.52:18888",
  "39.106.202.102:18888",
]

  passive = [
    # Passive accept connection in any case
    # Sample entries:
    # "ip:port",
    # "ip:port"
  ]

  http {
    fullNodePort = 8090
    solidityPort = 8091
  }

  rpc {
    port = 50051

    # Number of gRPC thread, default availableProcessors / 2
    # thread = 16

    # The maximum number of concurrent calls permitted for each incoming connection
    # maxConcurrentCallsPerConnection =

    # The HTTP/2 flow control window, default 1MB
    # flowControlWindow =

    # Connection being idle for longer than which will be gracefully terminated
    maxConnectionIdleInMillis = 60000

    # Connection lasting longer than which will be gracefully terminated
    # maxConnectionAgeInMillis =

    # The maximum message size allowed to be received on the server, default 4MB
    # maxMessageSize =

    # The maximum size of header list allowed to be received, default 8192
    # maxHeaderListSize =

    # Transactions can only be broadcast if the number of effective connections is reached.
    minEffectiveConnection = 0
  }

}



seed.node = {
  # List of the seed nodes
  # Seed nodes are stable full nodes
  # example:
  # ip.list = [
  #   "ip:port",
  #   "ip:port"
  # ]
  ip.list = [
  "39.106.151.237:18888",
"39.106.164.249:18888",
"39.106.65.52:18888",
"39.106.202.102:18888",
  ]
}

genesis.block = {
  # Reserve balance
  assets = [
    {
      accountName = "G1"
      accountType = "AssetIssue"
      address = "TNNqZuYhMfQvooC4kJwTsMJEQVU3vWGa5u"
      balance = "0"
    },
    {
      accountName = "Blackhole"
      accountType = "AssetIssue"
      address = "T9yD14Nj9j7xAB4dbGeiX9h8unkKHxuWwb"
      balance = "-9223372036854775808"
    }
  ]

  witnesses = [
    {
      #address: 27QAUYjg5FXfxcvyHcWF3Aokd5eu9iYgs1c
      address: TB4B1RMhoPeivkj4Hebm6tttHjRY9yQFes
      url = "http://Mercury.org",
      voteCount = 105
    },
    {
      #address: 27g8BKC65R7qsnEr2vf7R2Nm7MQfvuJ7im4
      address: TT1smsmhxype64boboU8xTuNZVCKP1w6qT
      url = "http://Venus.org",
      voteCount = 104
    },
        {
      #address: 27g8BKC65R7qsnEr2vf7R2Nm7MQfvuJ7im4
      address: TXtrbmfwZ2LxtoCveEhZT86fTss1w8rwJE
      url = "http://Venus.org",
      voteCount = 104
    },
            {
      #address: 27g8BKC65R7qsnEr2vf7R2Nm7MQfvuJ7im4
      address: TWKKwLswTTcK5cp31F2bAteQrzU8cYhtU5
      url = "http://Venus.org",
      voteCount = 104
    },

  ]

  timestamp = "0" #2017-8-26 12:00:00

  parentHash = "0xd9ef39a23e4d564cde288df2e1b505a7b22480d40d7c5f153cf8fe3cebfe622c"
}

#localwitness = [
#]

localwitnesskeystore = [
  // "src/main/resources/localwitnesskeystore.json"
]

block = {
  needSyncCheck = true
  maintenanceTimeInterval = 300000 // test: 20000
  proposalExpireTime = 300000// 3 day: 259200000(ms) test:240000
}

vm = {
  supportConstant = true
  minTimeRatio = 0.0
  maxTimeRatio = 5.0
  saveInternalTx = true

  # In rare cases, transactions that will be within the specified maximum execution time (default 10(ms)) are re-executed and packaged
  # longRunningTime = 10
}

committee = {
  chargingSwitchOn = 0
}

sidechain = {
  chargingType = 0   //0:trx, 1:sun_token
  chargingBandwidth = 0   //0:off, 1:on  if committee.chargingSwitchOn == 0, chargingBandwidth is always off
  energyFee = 1 // 1 sun per energy
  totalEnergyLimit = 100000000000 // 100_000_000_000 frozen energy limit
  maxCpuTimeOfOneTx = 50 // max cpu time to execute single smart contract transaction. default 50ms. proposal 13

  # mandatory to have sideChainId
  sideChainId = "41CE287AB7EA16176F0D368A7218BBE76220FA765F"//TTGhuSDKr561gzHFjkZ1V4ZtMgUEFLa7ct
  mainChainGateWayList = ["TUmGh8c2VcpfmJ7rBYq1FU9hneXhz3P8z3"]

}

log.level = {
  root = "INFO" // TRACE;DEBUG;INFO;WARN;ERROR
}